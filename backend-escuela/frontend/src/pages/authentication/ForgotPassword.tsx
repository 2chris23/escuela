import { Box, Typography } from '@mui/material';

const ForgotPassword = () => {
  return (
    <Box>
      <Typography variant="h4">Forgot Password Page</Typography>
    </Box>
  );
};

export default ForgotPassword; 